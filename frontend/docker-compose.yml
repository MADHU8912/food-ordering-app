version: "3.8"

services:
  backend:
    build: ./backend
    container_name: food-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: food-frontend
    ports:
      - "8081:80"
    depends_on:
      - backend